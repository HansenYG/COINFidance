# Allow pip to fall back to the PyTorch CPU wheel index alongside PyPI.
# This pulls a ~190 MB CPU-only torch instead of the 700 MB CUDA build,
# which fits Render's free-tier disk + memory.
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi>=0.115.0,<0.120
uvicorn[standard]==0.30.6
pydantic>=2.11.7,<3.0
python-dotenv==1.0.1
requests==2.32.3
supabase==2.28.0
numpy>=2.1,<3.0
torch==2.4.1
python-multipart==0.0.12

# Deepfake detection — quantized ONNX ViT, runs on CPU via onnxruntime.
# Designed to fit Render's 512 MB free tier (vs torch+transformers which
# OOMs). Model file is auto-downloaded on first startup (~89 MB).
onnxruntime>=1.18,<2.0
opencv-python-headless>=4.10,<5.0
Pillow>=10.4,<12.0
